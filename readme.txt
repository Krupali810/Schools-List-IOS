To run the project, please open the xcodeproj file. 

The project incorporates two approaches to fetching information: using static files and using API calls. 
To use static files, please open ViewController.swift and change the isDemo variable to true, else false (default) to fetch data through networking API. 
